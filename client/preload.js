window.ipcRenderer = require('electron').ipcRenderer;
window.electron = require('electron')
window.remote = require('electron').remote
window.EventEmitter = require('events');

// window.eMenu = window.remote.require('menu')